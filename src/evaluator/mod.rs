use crate::data::token::SeparatedToken;
use crate::data::token::Token;
//use crate::data::object::Object;

// TODO: this function is generate Object from SeparatedToken. The Object is contained by crate::data::object, and used to express program's process. evaluator will evaluate this Object.
fn generate_object(separated_tokens: Vec<SeparatedToken>) {}
